# Definimos el número hasta el que queremos contar (por ejemplo, 5)
numero_limite = 5

# Usamos 'for' con range(N)
print(f"Contando desde 0 hasta {numero_limite - 1}:")
for i in range(numero_limite):
    print(i)
